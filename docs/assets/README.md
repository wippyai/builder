# Repository images

`banner.svg` is the banner used by the README; `banner.png` is its 2× raster export.
The banner uses the shared Wippy dark background and gold accent.

Builder-owned artwork is MIT licensed.
